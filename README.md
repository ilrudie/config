# Config

## Installation

```bash
curl -L https://github.com/ilrudie/config/archive/refs/heads/main.zip -o config.zip 
unzip config.zip 
cd config-main 
brew bundle --file=brew/Brewfile
```

This installs: tmux, lazyvim, docker, go (1.26.1), rust (1.94.1), jq, and kind.
