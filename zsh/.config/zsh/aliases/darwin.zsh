## macOS-specific aliases — sourced after common.zsh when $OSTYPE == darwin*.

# Clipboard primitives used by the cross-platform aliases (gdx, xc, ...).
alias clip='pbcopy'
alias clip-paste='pbpaste'
